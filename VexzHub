-- Fast Attack + Animation Cancel — MAX SPEED — by Quoc Hoa
_G.FastAttack = true

local Players           = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService        = game:GetService("RunService")
local VIM               = game:GetService("VirtualInputManager")
local LocalPlayer       = Players.LocalPlayer

local HitRemote = ReplicatedStorage:FindFirstChild("Modules")
    and ReplicatedStorage.Modules:FindFirstChild("Net")
    and ReplicatedStorage.Modules.Net:FindFirstChild("RE/RegisterHit")

local hash = "079baa9c"
pcall(function()
    if not HitRemote then return end
    local mt = getrawmetatable(game)
    local old = mt.__namecall
    setreadonly(mt, false)
    mt.__namecall = newcclosure(function(self, ...)
        if getnamecallmethod() == "FireServer" and self == HitRemote then
            local a = {...}
            if type(a[4]) == "string" and #a[4] >= 6 then hash = a[4] end
        end
        return old(self, ...)
    end)
    setreadonly(mt, true)
end)

local ATTACK_KEYWORDS = {
    "attack", "slash", "punch", "kick", "hit",
    "swing", "strike", "combo", "skill", "cast",
    "shoot", "throw", "smash", "dash", "ability",
    "stab", "cleave", "chop", "jab", "uppercut",
    "m1", "light", "heavy", "slam", "spin", "m2",
}

local function isAttackAnim(track)
    local id   = tostring(track.Animation and track.Animation.AnimationId or ""):lower()
    local name = track.Name:lower()
    for _, kw in ipairs(ATTACK_KEYWORDS) do
        if id:find(kw) or name:find(kw) then return true end
    end
    return false
end

-- Hủy Animation + Animator
local function cancelAll()
    local char = LocalPlayer.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end

    -- Hủy Animation Track
    for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
        if isAttackAnim(track) then
            track:Stop(0)
        end
    end

    -- Hủy Animator trong character (phòng thủ)
    for _, anim in ipairs(char:GetDescendants()) do
        if anim:IsA("Animator") then
            pcall(function() anim:Destroy() end)
        end
    end
end

local function getRoot()
    local c = LocalPlayer.Character
    return c and c:FindFirstChild("HumanoidRootPart")
end

-- Cache mobs
local cachedMobs = {}
local mobUpdateFrame = 0

-- ═══════════════════════════════════════════
-- LOOP CHÍNH: RenderStepped = 60 FPS
-- Mỗi frame: Cancel + Click ×3 + FireServer
-- ═══════════════════════════════════════════
RunService.RenderStepped:Connect(function()
    if not _G.FastAttack then return end

    -- Cancel animation
    cancelAll()

    -- Click ảo ×3
    for _ = 1, 3 do
        VIM:SendMouseButtonEvent(0, 0, 0, true,  game, 1)
        VIM:SendMouseButtonEvent(0, 0, 0, false, game, 1)
    end

    -- Cập nhật mob mỗi 2 frame
    mobUpdateFrame += 1
    if mobUpdateFrame >= 2 then
        mobUpdateFrame = 0
        local r = getRoot()
        if r then
            local E = workspace:FindFirstChild("Enemies")
            cachedMobs = {}
            if E then
                for _, e in ipairs(E:GetChildren()) do
                    if not e:IsA("Model") then continue end
                    local h  = e:FindFirstChildOfClass("Humanoid")
                    local er = e:FindFirstChild("HumanoidRootPart")
                    if h and h.Health > 0 and er and (r.Position - er.Position).Magnitude <= 200 then
                        table.insert(cachedMobs, er)
                    end
                end
            end
        end
    end

    -- FireServer mỗi frame
    for _, mr in ipairs(cachedMobs) do
        pcall(function()
            HitRemote:FireServer(mr, {}, hash)
        end)
    end
end)

-- ═══════════════════════════════════════════
-- LOOP PHỤ: Cancel không delay (nhanh nhất)
-- ═══════════════════════════════════════════
task.spawn(function()
    while _G.FastAttack do
        cancelAll()
    end
end)

print("[FastAttack MAX] ON | Hash:", hash)
print("[FastAttack MAX] OFF: _G.FastAttack = false") 
